{{- $additionalPorts := (include "timestamp-formatter-connector.customization.ports" $) }}
apiVersion: v1
kind: Service
metadata:
  labels:
    {{- include "timestamp-formatter-connector.labels" . | nindent 4 }}
  name: timestamp-formatter-connector-service
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      protocol: "TCP"
    {{- if $additionalPorts }}
      {{- $additionalPorts | nindent 4 }}
    {{- end }}
  selector:
    {{- include "timestamp-formatter-connector.selectorLabels" . | nindent 4 }}
